# This directory must be the root of a mounted, encrypted local backup volume.
# Update the matching RequiresMountsFor and ReadWritePaths entries in the unit
# if the selected mount path differs.
FAMILY_FINANCE_BACKUP_ROOT=/mnt/familyfin-backup
