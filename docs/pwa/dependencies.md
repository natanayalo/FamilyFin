# PWA prerequisites and feature-module ownership

Status: T01 API/auth foundation and T02 frontend shell are merged; financial feature modules remain planned. T11 adds a private deployment and operations baseline. T00 established the ownership plan.

## Repository prerequisites

T01 foundation progress: API-owned `BEGIN IMMEDIATE` transaction participation, atomic idempotency response storage, FastAPI lifecycle, two-account authentication/session services, request/origin/host controls, typed transport errors, safe health/status, and bounded route-aware upload transport policy are implemented. JSON/unlisted bodies remain capped at 1 MiB; planned multipart routes add only their parser's file limit plus bounded overhead. No financial mutation route is registered or replay-safe. Remote mutating automation and attention-file commits remain blocked on the documented readiness and per-run verified pre-import backup remediation.

T11 deployment progress: checked-in Linux systemd/Caddy reference configuration, loopback-only service boundaries, tailnet ACL/Serve activation guidance, host verification checks, and an encrypted-volume daily backup template with manual retention/restore procedures are provided in [deployment.md](deployment.md). Actual host, tailnet identities/device selectors, MagicDNS hostname, encrypted backup destination, and retention evidence owner remain activation decisions. No host or tailnet configuration is changed by this repository.

| Area | Present state | Required before feature modules |
| --- | --- | --- |
| Python | Python >=3.12,<3.13; uv project; current runtime dependencies include FastAPI, Uvicorn, multipart parsing, SQLAlchemy, Alembic, Pydantic, pandas, openpyxl, Plotly, and Streamlit | Keep current Python lock and financial packages during rollout. Remove the Streamlit UI/dependency in T12 only after its acceptance gate; keep the Python CLI. |
| Database | SQLite, SQLAlchemy 2, WAL, BEGIN IMMEDIATE write transactions, Alembic migrations through current head 0012 | Keep one active data root and current archive directories. Continue forward-only migrations and apply them through the existing database initialization path. |
| Frontend | `frontend/` contains the pinned Node 22.14.0/Next App Router workspace, API client, sign-in flow, shell, design system, RTL assets, manifest, and static-only service worker | Keep exact runtime/package pins and local asset policy. Do not copy the upstream lockfile wholesale or cache API responses, HTML, files, or form state. |
| HTTP | FastAPI app/lifecycle, `/api/v1` health and authentication/session routes, request IDs, typed errors, no-store/security headers, trusted-host/origin checks, CSRF, bounded requests, two equal-permission account service, host bootstrap/recovery CLI, actor audit metadata, and API-owned SQLite idempotency foundation are implemented | Add feature routers only after contracts are stable. Require `authenticated_router()` for household routes. Idempotency result and financial commit must be atomic; filesystem effects need a separate verified recovery protocol. Until a route passes its route-specific gate it is outcome-unknown and cannot be automatically retried. |
| Deployment | Linux systemd units for the API, PWA, and verified backup job; a loopback Caddy same-origin proxy; Serve/ACL guidance; host checks and manual recovery runbook are included | Select an always-on host, dedicated tailnet device/hostname and two household identities, device restrictions, encrypted backup volume, and evidence owner. Install and validate the sample configuration on that host; expose only HTTPS through Tailscale Serve and keep Funnel disabled. |
| PWA | Manifest, local install icons, offline shell, reconnect state, and static-only service worker are implemented in T02 | Keep financial views online-only; exclude HTML with personal data, all `/api/` responses, files, and form state from persistent browser storage. |
| Source pin | Preferred Shadcn Fintech audit is pinned to e338240d10c908ab49403adbcd0bb4cc980ebfce | Use only this exact source revision if code is copied. Retain MIT attribution and review third-party asset/dependency licenses. See [template-audit.md](template-audit.md). |
| CI | GitHub Actions runs Python lint/tests, migration cycle and import-capacity check, plus frontend install/tests/lint/typecheck/build and browser acceptance | Add contract and browser flow coverage with each feature module; keep the Python and capacity jobs. |

The exact API shapes are in [api-contracts.md](api-contracts.md); feature parity and acceptance scenarios are in [ui-parity-matrix.md](ui-parity-matrix.md). The table distinguishes implemented foundations from remaining prerequisites.

## Ownership model for follow-up PRs

T01 owns the Python API/authentication services and transport contracts. T02 owns the browser API client and every frontend surface, including sign-in/session handling, shell, and design system. Both foundations are merged. After the foundation, data modules can proceed independently. One PR should not edit another feature module’s DTOs or financial services without coordinating its owner.

Suggested structure:

- Python adapters: src/family_finance/api/routers/<feature>.py and DTOs in src/family_finance/api/schemas/
- Frontend: frontend/src/features/<feature>/, shared shell/components in frontend/src/components/
- Shared versioned contract: docs/pwa/api-contracts.md and a generated OpenAPI document from the actual adapter once implemented
- Financial logic stays in its present domain modules; route handlers call the exact services listed in the parity matrix.

| Follow-up module / owner | Primary scope | Existing Python ownership | PWA route ownership | Depends on | Can proceed independently when |
| --- | --- | --- | --- | --- | --- |
| T01. Python API, auth, and host foundation | FastAPI app/lifecycle, /api/v1, exactly two app identities with equal permissions, server-side sessions, CSRF, actor identity policy, no-store, request IDs, atomic idempotency unit of work, error mapping, body limits, safe health/status, Tailscale deployment guide | New Python transport/auth package wrapping existing services | `/api/v1/health`; `/api/v1/auth/session` sign-in/session/sign-out only; feature router helper; no financial routes | None; first prerequisite | Foundation complete when two local accounts are bootstrapped securely, sessions can be revoked, host/origin/CSRF/rate/body policies are tested, and service transaction/replay behavior passes. Each financial mutation still needs its own atomicity or recovery evidence before automatic retries are advertised. |
| T02. PWA shell and design system | Pin/upstream attribution, Next workspace, frontend API client, sign-in screens, session handling, global errors, mobile shell, Hebrew/RTL root, local Hebrew font, navigation, responsive table/form patterns, install manifest, static-only service worker | None | All frontend ownership, including API client, sign-in/session UX, shell, design system, and route stubs for all nine pages | Documented API/auth contracts; template audit | Can start in parallel with T01 using documented contracts and synthetic fixtures. T02 does not wait for live endpoints and must not add financial mock data to production navigation. |
| 2. Overview and Expenses | Filters, headline metrics, comparisons, charts, category behavior/trends, contributors, recurring/anomaly drill-down | DashboardService, MetricsService | /dashboard, /expenses | API foundation; shell | API contracts stable; can deliver as independent read-only PR. |
| 3. Data Quality, imports, reconciliation | Quality panel, XLSX preview/decision plan, download, commit, history, reconciliation resolution | DashboardService, ImportService, parser, repositories | /data-quality | API foundation; shell | Upload/error/token contracts stable. Coordinate with classification only on shared navigation/issue links. |
| 4. Classification | Filtered queue, overrides, clearing, reusable-rule preview/create/history/disable | ClassificationService | /classification | API foundation; shell; Data Quality link only | API contract stable. Concurrent-write policy for overrides/rules is explicitly documented before claiming protection. |
| 5. Planning | Manual scenario, historical/CSV seed previews, mapping acknowledgement, revision editor, restore/clone/archive, projection and comparisons | PlanningService, MetricsService | /planning | API foundation; shell; import/data-quality read access | API contract stable; can develop independently from forecast editor. |
| 6. Net Worth | Account registry, observed snapshots, strict CSV, immutable revisions, account history, aggregates, trend, forecast comparison endpoint | NetWorthService | /net-worth | API foundation; shell | API contract stable. Forecast comparison view may start after Forecast read API exists. |
| 7. Savings Forecast | Scenario/revision pin, starting pools, Net Worth bridge, three case editor, projection, save and existing forecast lifecycle service methods | SavingsForecastService, PlanningService, NetWorthService | /savings-forecast | API foundation; shell; Planning; Net Worth for snapshot seeding | API DTOs include exact planning revision and net-worth source provenance. |
| 8. Apartment Plan | Forecast pin, study lifecycle/revisions, guardrails, alternatives, projection/readiness, mortgage schedule, save | ApartmentPlanningService, SavingsForecastService, PlanningService | /apartment-plan | API foundation; shell; Forecast; Planning | API carries exact forecast revision/hash. |
| 9. Automation & Insights | Audit status, dry-run, preferences, attention files, alerts, monthly summaries/downloads; mutating automation only after mandatory verified-backup remediation | AuditService, AutomationService, InsightsService, existing AutomationRunRow | /automation-insights | API foundation; shell; Import/Planning/Forecast/Net Worth/Apartment; remediation enforced and verified across AutomationService entry points before any mutating remote route | Read-only status and dry-runs can ship earlier. Keep remote run-now and attention-file commit blocked until every mutating run across Streamlit, CLI, and API verifies readiness and creates its own successful pre-import backup before processing. Do not expose raw server paths. This gate does not block T01 or T02. |
| 10. Operations continuity | Host runbook, audit and backup verification access, manual restore practice, backup destination/retention | AuditService, BackupService, CLI | Current CLI remains; optional /settings/operations only by explicit product decision | API foundation if web operations route is approved; host decision | CLI path can be documented independently. No automatic restore implementation in this module. |
| T11. Deployment and private operations | Production process/proxy configuration, same-origin Tailscale Serve, tailnet-only checks, encrypted-volume backup schedule, retention, restore, and recovery verification | Existing API CLI, AuditService, BackupService | None | Merged T01 API and T02 PWA; host, tailnet, and backup destination decisions | Reference files and credential-free artifact checks are in place. Household activation waits for the host/tailnet/backup decisions and live private-access/restore evidence. No host mutation or automatic restore is included. |
| T12. Streamlit retirement | Remove Streamlit UI and its hosting configuration only after the final parity and recovery acceptance gate passes; keep the Python CLI | Current Streamlit UI; CLI remains outside the retired web UI | Retirement only; no replacement financial logic | All parity modules; mandatory backup readiness; successful restore practice; verified private access; explicit gate evidence/sign-off | Final acceptance verifies every matrix workflow, backup readiness and recovery, two-user permissions, tailnet-only access, and no financial-data offline behavior. Until it passes, Streamlit remains usable as the fallback. |

Each module PR owns its route adapter, feature DTO mapping, Hebrew labels, loading/empty/error states, accessibility/mobile behavior, and acceptance coverage for its matrix rows. The core services remain shared, not duplicated per feature.

## Dependency sequence

1. Review this contract; identity bootstrap/recovery and Node server mode are implemented. Select the host, tailnet name/ACL owner and household identities/devices, encrypted backup destination, retention evidence owner, and whether operations controls belong in PWA.
2. Keep T01 API/auth and T02 frontend shell as the compatibility foundation. Keep session identity separate from Tailscale identity. Do not route private data through an external auth or analytics provider.
3. Land independent read-only modules (Overview/Expenses, Data Quality) and then independent write modules (Classification, Planning, Net Worth) once error/Decimal/token contracts are frozen.
4. Land Savings Forecast after Planning and Net Worth; Apartment after Forecast. Ship Automation & Insights read-only status/dry-runs separately from remote mutations; do not expose mutating automation until mandatory per-run verified backup readiness and a successful pre-import backup have been implemented and verified.
5. Run the PWA alongside Streamlit and the CLI using the same database and archives. Compare each service output before the PWA becomes the primary entry point.
6. Verify private tailnet HTTPS, two-user login separation, mobile installability, offline denial of financial views/mutations, mandatory backup readiness for mutating automation, backup verification, and documented manual restore before household rollout. T12 retires Streamlit only after this final parity/recovery gate passes; keep the CLI.

The sequence does not authorize a financial schema rewrite. Existing service logic and financial invariants remain the compatibility target.
